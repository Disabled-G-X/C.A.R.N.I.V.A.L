# C.A.R.N.I.V.A.L — core dependencies
# Pinned where sensible; minimum versions otherwise.
flask>=3.0
flask-socketio>=5.3
python-socketio>=5.11
python-engineio>=4.9
google-genai>=1.0
python-dotenv>=1.0
psutil>=5.9
requests>=2.31

# Web search fallback (used by the web_search skill).
# `ddgs` is the maintained rename of duckduckgo_search; the skill supports both.
ddgs>=9.0
duckduckgo-search>=6.0

# Optional / on-demand (imported lazily — install only what you use):
#   edge-tts             -> server-side TTS alternative to browser voice
#   pyttsx3              -> offline TTS
#   miniaudio, sounddevice -> Edge TTS / Whisper audio playback
#   speechrecognition    -> headless STT (--voice mode)
#   faster-whisper, numpy -> offline STT
#   wikipedia            -> wikipedia skill
#   deep-translator      -> translate skill
#   python-kasa          -> smart_home skill (TP-Link Kasa)
#   mss (or pyautogui)   -> screenshot skill
#   pyperclip            -> clipboard skill
#   pynvml               -> GPU temperature in dashboard
